# vv-browser-manager / library

Bindable layer — stateless Ruby with no Rails dependencies.

## Contents

- `ModelRegistry` — thread-safe in-memory registry (Monitor-based, pure Ruby)
- `LlamaStack::ResponseFormatter` — pure data transformation for OpenAI-compatible responses
